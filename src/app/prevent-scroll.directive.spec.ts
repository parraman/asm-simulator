import { PreventScrollDirective } from './prevent-scroll.directive';

describe('PreventScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventScrollDirective();
    expect(directive).toBeTruthy();
  });
});
